

<h2>Enter url that you want to shorten</h2>

<%= form_with model: @url, url: urls_path, local: true do |form| %>

 <% if @url.errors.any? %>
    <%= @url.errors.any? %>

    <%# TODO display error validation msgs %>
    <div id="error_explanation">
        <h2><%= pluralize(@url.errors.count, "error") %> prohibited this URL from being saved:</h2>
        <ul>
        <% @url.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
    </div>
    <% end %>
    <div class="field form-group d-flex flex-row">
        <%= form.text_field :original_url, placeholder: "Enter url to shorten", class:"mx-2 field form-control p-3" %>
        <%= form.submit "Shorten URL", class: "btn btn-secondary ml-2" %>
    </div>
<% end %>

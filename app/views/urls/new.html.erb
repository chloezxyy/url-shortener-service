<h1>NotSoTinyUrl</h1>

<span>Enter url that you want to shorten</span>

<%= form_with model: @url, url: urls_path, local: true do |form| %>

 <% if @url.errors.any? %>
    <%= @url.errors.any? %>

    <%# TODO display error validation msgs %>
    <div id="error_explanation">
        <h2><%= pluralize(@url.errors.count, "error") %> prohibited this URL from being saved:</h2>
        <ul>
        <% @url.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
        </ul>
    </div>
    <% end %>
    <%= form.label "Your long url" %>
    <%= form.text_field :original_url, placeholder: "Enter url to shorten" %>

    <%= form.submit "Shorten URL" %>

<% end %>
